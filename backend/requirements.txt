fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Redis (pub/sub + cache)
redis[hiredis]==5.2.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12

# Storage (Cloudflare R2 via boto3 S3-compatible API)
boto3==1.35.0

# Validation
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Scheduler (order expiry)
apscheduler==3.10.4

# LiveKit (voice call token generation)
livekit-api==0.7.1

# APNs VoIP push (iOS call notifications) — http2=True requires the h2 extra
httpx[http2]==0.28.0

# Rate limiting (Redis-backed, works across multiple workers)
slowapi==0.1.9

# Utils
python-dotenv==1.0.1

# Firebase Cloud Messaging
firebase-admin==6.5.0
